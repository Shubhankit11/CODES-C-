#include<stdio.h>
#include<math.h>
int main()
{
	double x1=2.0, x2=3.6, x3=-3.3;
	printf("truncated value is %f \n", trunc(x1));
	printf("truncated value is %f \n", trunc(x2));
	printf("truncated value is %f \n", trunc(x3));
	return 0;
}
